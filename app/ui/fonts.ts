import { Inter, Lusitana } from 'next/font/google'; // Primary and secondary fonts
 
export const inter = Inter({ subsets: ['latin'] }); // Subset I want to load
export const lusitana = Lusitana({ weight: ['400', '700'], subsets: ['latin'] });